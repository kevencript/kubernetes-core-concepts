apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-pvc
  namespace: database
spec:
  resources:
    requests:
      storage: 1Gi
  #volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
